# cargo-audit configuration
# https://rustsec.org/

[advisories]
# Unmaintained transitive dependencies we cannot fix without upstream releases.
ignore = [
    # bincode 1.3.3 — transitive via imagepipe 0.5.0 (RAW camera processing)
    "RUSTSEC-2025-0141",
    # paste 1.0.15 — transitive via rav1e, metal, accesskit_windows
    "RUSTSEC-2024-0436",
    # yaml-rust 0.4.5 — transitive via imagepipe -> serde_yaml
    "RUSTSEC-2024-0320",
]
